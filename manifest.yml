applications:
  - name: CompeTrack
    buildpack: java_buildpack
    path: build/libs/Competrack-0.0.2-SNAPSHOT.jar
    services:
      - CompeTrackNew-db
    env:
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 14.0.1_8}}'